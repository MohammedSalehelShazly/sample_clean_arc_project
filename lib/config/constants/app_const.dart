import 'package:flutter/foundation.dart';

class AppConst {
  const AppConst();

  static const isDebug = kDebugMode;

  static const defaultIcBtnSize = 40.0;
  static const defaultCountryCode = '+966';
  static const maxPhoneNumLen = 9;
  static const duration700ms = Duration(milliseconds: 700);
  static const switchWidgetDur = duration700ms;
}
